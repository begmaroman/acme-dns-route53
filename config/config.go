package config

// Config contains the configuration data
// TODO: Implement
type Config struct {
}

// Init initializes Config structure based on the given data
func Init() (*Config, error) {
	return &Config{}, nil
}
